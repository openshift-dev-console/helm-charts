apiVersion: v1
kind: Service
metadata:
  name: {{ include "101-chart_without_values.fullname" . }}
  labels:
    {{- include "101-chart_without_values.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "101-chart_without_values.selectorLabels" . | nindent 4 }}
