apiVersion: v1
kind: Service
metadata:
  name: {{ include "102-chart_with_one_opt_string.fullname" . }}
  labels:
    {{- include "102-chart_with_one_opt_string.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "102-chart_with_one_opt_string.selectorLabels" . | nindent 4 }}
