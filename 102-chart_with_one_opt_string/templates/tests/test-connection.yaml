apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "102-chart_with_one_opt_string.fullname" . }}-test-connection"
  labels:
    {{- include "102-chart_with_one_opt_string.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "102-chart_with_one_opt_string.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
